Install the DIAG1B APK. Take one tungsten-light frame. Expected sidecar: /DCIM/Camera/IMG_YYYYMMDD_HHMMSS_M10R_CAPTURE1B.json. If the file cannot be written, Photon log must contain `M10-R DIAG1B JSON WRITE FAILED; payload=` followed by the complete diagnostics JSON. Upload either sidecar or the post-capture Photon log with the matching JPEG.
