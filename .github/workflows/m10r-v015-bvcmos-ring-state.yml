name: M10-R v0.15 BvCMOS ring and source-state writers
on:
  push:
    branches: [m10r-metering-latch-trace]
    paths: ['.github/workflows/m10r-v015-bvcmos-ring-state.yml', '.m10r-v015-bvcmos-ring-state-trigger']
permissions:
  contents: read
jobs:
  trace:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m pip install --quiet capstone
      - name: Recover firmware and trace ring/state writers
        shell: bash
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 --retry-delay 2 'https://reddotforum.com/Firmware_files/M10-R/M10-R-30.22.23.34-Customer.FW' -o /tmp/M10-R.FW
          echo 'ec8df72dc7d4c90332abb3cdbc07c6db837ffed802c1a9712b6f3d6e0edf6498  /tmp/M10-R.FW' | sha256sum -c -
          cat >/tmp/unpack.cpp <<'CPP'
          #include <cstdint>
          #include <fstream>
          #include <stdexcept>
          #include <vector>
          using namespace std; static uint32_t u32(const vector<uint8_t>&b,size_t o){return uint32_t(b[o])|(uint32_t(b[o+1])<<8)|(uint32_t(b[o+2])<<16)|(uint32_t(b[o+3])<<24);} struct BR{const uint8_t*p;size_t n,bit=0;int get(){if(bit>=n*8)throw runtime_error("eof");int v=(p[bit>>3]>>(7-(bit&7)))&1;++bit;return v;}}; static int cat(BR&r){int a=r.get();if(!a)return r.get()?7:6;int b=r.get();if(!b)return r.get()?5:1;int c=r.get();if(!c)return r.get()?2:0;int d=r.get();if(!d)return 4;return r.get()?8:3;} int main(){ifstream f("/tmp/M10-R.FW",ios::binary);vector<uint8_t>fw((istreambuf_iterator<char>(f)),{});const size_t C=0x38;uint32_t h=u32(fw,C+8),expected=u32(fw,C+0x10);const uint8_t*tr=&fw[C+0x54];vector<uint8_t>out;out.reserve(expected);size_t st=C+h;for(int i=0;i<8;i++){size_t e=st+i*16;uint32_t ro=u32(fw,e+4),ps=u32(fw,e+8),us=u32(fw,e+12);BR br{&fw[C+ro],ps};for(uint32_t j=0;j<us;j++){int k=cat(br),x=0,v=0;for(int z=0;z<k;z++)x=(x<<1)|br.get();if(k)v=(x>=(1<<(k-1)))?x:x-((1<<k)-1);out.push_back(tr[v+128]);}}ofstream o("/tmp/M10R_unpacked.bin",ios::binary);o.write((char*)out.data(),out.size());return out.size()==expected?0:3;}
          CPP
          g++ -O3 -std=c++17 /tmp/unpack.cpp -o /tmp/unpack && /tmp/unpack
          python3 tools/m10r_sections.py /tmp/M10R_unpacked.bin /tmp/sections >/dev/null
          python3 - <<'PY' >/tmp/m10r-v015-bvcmos-ring-state.txt
          from pathlib import Path
          import struct,re
          from capstone import Cs,CS_ARCH_ARM,CS_MODE_THUMB,CS_MODE_LITTLE_ENDIAN
          from capstone.arm import ARM_OP_MEM,ARM_REG_PC,ARM_OP_IMM
          b=Path('/tmp/sections/090_CTRL-System.bin').read_bytes();base=0x08000000;pfx=4
          md=Cs(CS_ARCH_ARM,CS_MODE_THUMB|CS_MODE_LITTLE_ENDIAN);md.detail=True;md.skipdata=True
          def off(v):return pfx+v-base
          def dis(label,s,e):
            print(f'\n=== {label} {s:08x}-{e:08x} ===')
            for i in md.disasm(b[off(s):off(e)],s):
              if i.mnemonic!='.byte':print(f'{i.address:08x}: {i.mnemonic:8s} {i.op_str}')
          def literals(value):
            n=struct.pack('<I',value);q=0;out=[]
            while True:
              q=b.find(n,q)
              if q<0:break
              out.append(base+q-pfx);q+=1
            return out
          def refs_to_value(value):
            L=set(literals(value));out=[]
            for i in md.disasm(b[pfx:],base):
              if i.mnemonic=='.byte':continue
              for op in i.operands:
                if op.type==ARM_OP_MEM and op.mem.base==ARM_REG_PC:
                  a=((i.address+4)&~3)+op.mem.disp
                  if a in L:out.append((i.address,a,i.mnemonic,i.op_str))
            return out
          vals=[0x200190ac,0x2001adbb,0x2001adae,0x20000132]
          for v in vals:
            print('\nVALUE',hex(v),'LITERALS',[hex(x) for x in literals(v)])
            rr=refs_to_value(v);print('REFS',[(hex(a),hex(l),m,o) for a,l,m,o in rr])
            for a,_,_,_ in rr:dis('REF_'+hex(v)+'_'+hex(a),max(base,a-220),min(base+len(b)-pfx,a+360))

          # Trace the ring index getter and all its callers.
          def callers(t):
            out=[]
            for i in md.disasm(b[pfx:],base):
              if i.mnemonic.startswith('bl') and i.operands and i.operands[0].type==ARM_OP_IMM and (i.operands[0].imm&~1)==(t&~1):out.append(i.address)
            return out
          for t in [0x08007ec0,0x0801d044]:
            print('\nCALLERS',hex(t),[hex(x) for x in callers(t)])
            dis('FUNC_'+hex(t),max(base,t-0x100),min(base+len(b)-pfx,t+0x300))
            for a in callers(t):dis('CALLER_'+hex(a),max(base,a-180),min(base+len(b)-pfx,a+260))
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: m10r-v015-bvcmos-ring-state
          path: /tmp/m10r-v015-bvcmos-ring-state.txt
          if-no-files-found: error
