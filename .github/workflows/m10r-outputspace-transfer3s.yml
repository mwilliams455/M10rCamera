name: M10-R output-space transfer 3S
on:
  push:
    branches: [m10r-outputspace-transfer1a]
    paths: ['.github/workflows/m10r-outputspace-transfer3s.yml']
permissions:
  contents: write
jobs:
  trace:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m pip install --quiet capstone
      - name: Resolve interpolation-gamma resource key
        shell: bash
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 --retry-delay 2 'https://reddotforum.com/Firmware_files/M10-R/M10-R-30.22.23.34-Customer.FW' -o /tmp/M10-R.FW
          echo 'ec8df72dc7d4c90332abb3cdbc07c6db837ffed802c1a9712b6f3d6e0edf6498  /tmp/M10-R.FW' | sha256sum -c -
          cat >/tmp/unpack.cpp <<'CPP'
          #include <cstdint>
          #include <fstream>
          #include <stdexcept>
          #include <vector>
          using namespace std; static uint32_t u32(const vector<uint8_t>&b,size_t o){return uint32_t(b[o])|(uint32_t(b[o+1])<<8)|(uint32_t(b[o+2])<<16)|(uint32_t(b[o+3])<<24);} struct BR{const uint8_t*p;size_t n,bit=0;int get(){if(bit>=n*8)throw runtime_error("eof");int v=(p[bit>>3]>>(7-(bit&7)))&1;++bit;return v;}}; static int cat(BR&r){int a=r.get();if(!a)return r.get()?7:6;int bb=r.get();if(!bb)return r.get()?5:1;int c=r.get();if(!c)return r.get()?2:0;int d=r.get();if(!d)return 4;return r.get()?8:3;} int main(){ifstream f("/tmp/M10-R.FW",ios::binary);vector<uint8_t>fw((istreambuf_iterator<char>(f)),{});const size_t C=0x38;uint32_t h=u32(fw,C+8),expected=u32(fw,C+0x10);const uint8_t*tr=&fw[C+0x54];vector<uint8_t>out;out.reserve(expected);size_t st=C+h;for(int i=0;i<8;i++){size_t e=st+i*16;uint32_t ro=u32(fw,e+4),ps=u32(fw,e+8),us=u32(fw,e+12);BR br{&fw[C+ro],ps};for(uint32_t j=0;j<us;j++){int k=cat(br),x=0,v=0;for(int z=0;z<k;z++)x=(x<<1)|br.get();if(k)v=(x>=(1<<(k-1)))?x:x-((1<<k)-1);out.push_back(tr[v+128]);}}ofstream o("/tmp/M10R_unpacked.bin",ios::binary);o.write((char*)out.data(),out.size());return out.size()==expected?0:3;}
          CPP
          g++ -O3 -std=c++17 /tmp/unpack.cpp -o /tmp/unpack && /tmp/unpack
          python3 tools/m10r_sections.py /tmp/M10R_unpacked.bin /tmp/sections >/dev/null
          python3 - <<'PY' > research/M10R_OUTPUTSPACE_TRANSFER3S.txt
          from pathlib import Path
          import struct,re
          from capstone import *
          from capstone.arm import *
          b=Path('/tmp/sections/092_IMG-System.bin').read_bytes();B=0x42000000;P=4
          md=Cs(CS_ARCH_ARM,CS_MODE_THUMB|CS_MODE_LITTLE_ENDIAN);md.detail=True
          def off(a):return P+a-B
          def ds(a,z):return list(md.disasm(b[off(a):off(z)],a))
          def call(i):
            try:
              if i.mnemonic in ('bl','blx') and i.operands and i.operands[0].type==ARM_OP_IMM:return i.operands[0].imm
            except:pass
          def lit(i):
            try:
              if i.mnemonic.startswith('ldr') and len(i.operands)>1 and i.operands[1].type==ARM_OP_MEM and i.operands[1].mem.base==ARM_REG_PC:
                la=((i.address+4)&~3)+i.operands[1].mem.disp;q=off(la)
                if 0<=q<=len(b)-4:return la,struct.unpack_from('<I',b,q)[0]
            except:pass
          def adr(i):
            try:
              if i.mnemonic=='adr' and len(i.operands)>1 and i.operands[1].type==ARM_OP_IMM:return ((i.address+4)&~3)+i.operands[1].imm
            except:pass
          def cstr(a,n=160):
            q=off(a)
            if not 0<=q<len(b):return None
            z=b.find(b'\0',q,min(len(b),q+n));z=(min(len(b),q+n) if z<0 else z)
            x=b[q:z]
            if x and all(32<=c<127 or c in (9,10,13) for c in x):return x.decode('latin1','replace')
          def fmt(i):
            s=f'{i.address:08x}: {i.mnemonic:10s} {i.op_str}';a=adr(i);x=lit(i);c=call(i)
            if a is not None:s+=f' ; ADR={a:#x} STR={repr(cstr(a))}'
            if x:s+=f' ; LIT {x[0]:#x}->{x[1]:#x} PTRSTR={repr(cstr(x[1]))}'
            if c is not None:s+=f' ; CALL={c:#x}'
            return s
          print('M10-R OUTPUTSPACE TRANSFER3S')
          print('\n=== INTERPOLATION GAMMA LOADER WITH RESOLVED ADR STRINGS ===')
          for i in ds(0x4215440c,0x42154464):print(fmt(i))
          print('\n=== RAW DATA/ASCII 0x42154464..0x42154520 ===')
          lo=off(0x42154464);hi=off(0x42154520)
          for q in range(lo,hi,16):
            x=b[q:q+16];a=B+q-P;asc=''.join(chr(c) if 32<=c<127 else '.' for c in x)
            print(f'{a:08x}: {x.hex()}  {asc}')
          print('\n=== ASCII STRINGS 0x42154300..0x42154680 ===')
          raw=b[off(0x42154300):off(0x42154680)]
          for m in re.finditer(rb'[ -~]{3,}',raw):print(hex(0x42154300+m.start()),repr(m.group().decode('latin1','replace')))
          print('\n=== NEXT FUNCTION CONTAINING +0xB4 HIT ===')
          for i in ds(0x421544d8,0x421545d0):print(fmt(i))
          print('\n=== GLOBAL INTERPOLATION/GAMMA RESOURCE STRINGS ===')
          for m in re.finditer(rb'[ -~]{4,}',b):
            t=m.group().decode('latin1','replace');tl=t.lower()
            if ('gamma' in tl or 'interpolation' in tl) and any(k in tl for k in ('b2y','gamma','interpolation','.bin','tbl','table')):
              print(hex(B+m.start()-P),repr(t[:260]))
          print('\n=== CALL 0x421A0FC0 OWNER/STRINGS ===')
          for i in ds(0x421a0f40,0x421a1080):print(fmt(i))
          raw=b[off(0x421a0e00):off(0x421a1200)]
          for m in re.finditer(rb'[ -~]{4,}',raw):print(hex(0x421a0e00+m.start()),repr(m.group().decode('latin1','replace')[:240]))
          print('\n=== DECISION GATE ===')
          print('Resolve exact asset/resource requested by 0x4215440C and whether its key contains output-space state. Distinguish fixed interpolation-gamma asset from color-space-selected asset.')
          PY
      - name: Commit report
        shell: bash
        run: |
          git config user.name 'github-actions[bot]'; git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add research/M10R_OUTPUTSPACE_TRANSFER3S.txt
          git commit -m 'research: resolve interpolation gamma resource key 3S' || exit 0
          git pull --rebase origin m10r-outputspace-transfer1a
          git push
