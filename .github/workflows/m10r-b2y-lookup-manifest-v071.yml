name: M10-R v0.71 corrected B2Y common lookup and record manifest
on:
  push:
    branches: [m10r-reference-transfer-rgb1e]
    paths:
      - 'tools/m10r_b2y_lookup_manifest_v071.py'
      - '.github/workflows/m10r-b2y-lookup-manifest-v071.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m pip install --quiet 'capstone==5.0.3'
      - name: Fetch verify decode extract
        shell: bash
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 --retry-delay 2 'https://reddotforum.com/Firmware_files/M10-R/M10-R-30.22.23.34-Customer.FW' -o /tmp/M10-R.FW
          echo 'ec8df72dc7d4c90332abb3cdbc07c6db837ffed802c1a9712b6f3d6e0edf6498  /tmp/M10-R.FW' | sha256sum -c -
          python3 - <<'PY'
          from pathlib import Path
          p=Path('.github/workflows/m10r-v135-wb-mode-switch.yml').read_text()
          a=p.index("cat >/tmp/unpack.cpp <<'CPP'\n")+len("cat >/tmp/unpack.cpp <<'CPP'\n")
          z=p.index("\n          CPP",a)
          Path('/tmp/unpack.cpp').write_text(p[a:z])
          PY
          g++ -O3 -std=c++17 /tmp/unpack.cpp -o /tmp/unpack && /tmp/unpack
          echo '859c936b6ac8efe0bc4ce7f05623013644fc5d8aee3c6835f09860f452adc2e0  /tmp/M10R_unpacked.bin' | sha256sum -c -
          python3 tools/m10r_sections.py /tmp/M10R_unpacked.bin /tmp/sec >/dev/null
      - name: Trace lookup and enumerate B2Y records
        shell: bash
        run: |
          set -euo pipefail
          rm -f research/M10R_v071_b2y_lookup_manifest.txt
          python3 tools/m10r_b2y_lookup_manifest_v071.py /tmp/sec "$GITHUB_WORKSPACE" | tee research/M10R_v071_b2y_lookup_manifest.txt
          test -s research/M10R_v071_b2y_lookup_manifest.txt
          grep -q '^V070_LOOKUP_CALLER_COUNT=' research/M10R_v071_b2y_lookup_manifest.txt
          grep -q '^V070_RECORD_COUNT=' research/M10R_v071_b2y_lookup_manifest.txt
          grep -q '^OVERALL_VERDICT=LOOKUP_CODE_AND_RECORD_MANIFEST_RECORDED_WITHOUT_FORCED_MAPPING$' research/M10R_v071_b2y_lookup_manifest.txt
      - uses: actions/upload-artifact@v4
        with:
          name: M10R-V071-B2Y-LOOKUP-MANIFEST
          path: research/M10R_v071_b2y_lookup_manifest.txt
      - name: Commit evidence
        run: |
          git config user.name 'github-actions[bot]'; git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add research/M10R_v071_b2y_lookup_manifest.txt
          git commit -m 'research: record v071 corrected B2Y lookup manifest' || exit 0
          git pull --rebase origin m10r-reference-transfer-rgb1e && git push
