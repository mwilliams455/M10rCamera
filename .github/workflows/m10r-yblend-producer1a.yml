name: M10-R Y BLEND PRODUCER1A
on:
  push:
    branches: [m10r-yblend-producer1a]
    paths:
      - '.github/workflows/m10r-yblend-producer1a.yml'
      - 'tools/m10r_yblend_producer1a.py'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  producer-evidence:
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - name: Install pinned research dependencies
        run: python3 -m pip install --quiet capstone==5.0.3 unicorn==2.1.3
      - name: Fetch and verify original firmware
        shell: bash
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 --retry-delay 2 'https://reddotforum.com/Firmware_files/M10-R/M10-R-30.22.23.34-Customer.FW' -o /tmp/M10-R.FW
          echo 'ec8df72dc7d4c90332abb3cdbc07c6db837ffed802c1a9712b6f3d6e0edf6498  /tmp/M10-R.FW' | sha256sum -c -
      - name: Decode using existing verified extractor
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path
          p=Path('.github/workflows/m10r-v135-wb-mode-switch.yml').read_text()
          marker="cat >/tmp/unpack.cpp <<'CPP'\n"
          a=p.index(marker)+len(marker)
          z=p.index("\n          CPP",a)
          Path('/tmp/unpack.cpp').write_text(p[a:z])
          PY
          g++ -O3 -std=c++17 /tmp/unpack.cpp -o /tmp/unpack
          /tmp/unpack
          echo '859c936b6ac8efe0bc4ce7f05623013644fc5d8aee3c6835f09860f452adc2e0  /tmp/M10R_unpacked.bin' | sha256sum -c -
          python3 tools/m10r_sections.py /tmp/M10R_unpacked.bin /tmp/sec > /tmp/section_inventory.txt
      - name: Reproduce audit and independently re-extract normal records
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p producer_evidence/results
          python3 tools/m10r_yblend_bit15_audit1a.py /tmp/sec --cases 1024 --out producer_evidence/results/register_emulation.json > producer_evidence/results/audit_stdout.txt
          python3 tools/m10r_ycc_calib_compact_v086.py /tmp/sec . > producer_evidence/results/v086_reextraction.txt
          git rev-parse HEAD > producer_evidence/results/source_commit.txt
          cp /tmp/section_inventory.txt producer_evidence/results/
          if [ -f tools/m10r_yblend_producer1a.py ]; then
            python3 tools/m10r_yblend_producer1a.py /tmp/sec . producer_evidence/results
          fi
      - name: Package private research inputs for offline analysis
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p producer_evidence/inputs/sections producer_evidence/tools producer_evidence/research producer_evidence/wheels
          cp /tmp/sec/* producer_evidence/inputs/sections/
          cp tools/m10r_*.py producer_evidence/tools/
          cp research/M10R_v0{61,61b,62,68,69,77,78,86,87,92,94,97}_*.txt producer_evidence/research/ 2>/dev/null || true
          python3 -m pip download --quiet --only-binary=:all: --no-deps --dest producer_evidence/wheels capstone==5.0.3 unicorn==2.1.3
          python3 - <<'PY'
          from pathlib import Path
          import hashlib,json
          p=Path('producer_evidence')
          entries=[]
          for f in sorted(p.rglob('*')):
              if f.is_file():
                  b=f.read_bytes()
                  entries.append({'path':str(f.relative_to(p)),'bytes':len(b),'sha256':hashlib.sha256(b).hexdigest()})
          (p/'manifest.json').write_text(json.dumps(entries,indent=2)+'\n')
          (p/'README.txt').write_text('Private research inputs. Firmware sections and dependency wheels are for local analysis, not redistribution in the final evidence package. Software register tests do not establish ISP pixel arithmetic. No application-source changes.\n')
          PY
      - uses: actions/upload-artifact@v4
        with:
          name: M10R-YBLEND-PRODUCER1A-PRIVATE-RESEARCH
          path: producer_evidence/
          retention-days: 7
          if-no-files-found: error
