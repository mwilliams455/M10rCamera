name: M10-R v0.28 metering mode names and ring provenance
on:
  push:
    branches: [m10r-metering-latch-trace]
    paths: ['.github/workflows/m10r-v028-meter-mode-names-ring-provenance.yml']
  workflow_dispatch:
permissions:
  contents: write
jobs:
  trace:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: m10r-metering-latch-trace
          fetch-depth: 0
      - run: python3 -m pip install --quiet capstone
      - name: Metering enum and ring provenance trace
        shell: bash
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 --retry-delay 2 'https://reddotforum.com/Firmware_files/M10-R/M10-R-30.22.23.34-Customer.FW' -o /tmp/M10-R.FW
          echo 'ec8df72dc7d4c90332abb3cdbc07c6db837ffed802c1a9712b6f3d6e0edf6498  /tmp/M10-R.FW' | sha256sum -c -
          cat >/tmp/unpack.cpp <<'CPP'
          #include <cstdint>
          #include <fstream>
          #include <stdexcept>
          #include <vector>
          using namespace std; static uint32_t u32(const vector<uint8_t>&b,size_t o){return uint32_t(b[o])|(uint32_t(b[o+1])<<8)|(uint32_t(b[o+2])<<16)|(uint32_t(b[o+3])<<24);} struct BR{const uint8_t*p;size_t n,bit=0;int get(){if(bit>=n*8)throw runtime_error("eof");int v=(p[bit>>3]>>(7-(bit&7)))&1;++bit;return v;}}; static int cat(BR&r){int a=r.get();if(!a)return r.get()?7:6;int b=r.get();if(!b)return r.get()?5:1;int c=r.get();if(!c)return r.get()?2:0;int d=r.get();if(!d)return 4;return r.get()?8:3;} int main(){ifstream f("/tmp/M10-R.FW",ios::binary);vector<uint8_t>fw((istreambuf_iterator<char>(f)),{});const size_t C=0x38;uint32_t h=u32(fw,C+8),expected=u32(fw,C+0x10);const uint8_t*tr=&fw[C+0x54];vector<uint8_t>out;out.reserve(expected);size_t st=C+h;for(int i=0;i<8;i++){size_t e=st+i*16;uint32_t ro=u32(fw,e+4),ps=u32(fw,e+8),us=u32(fw,e+12);BR br{&fw[C+ro],ps};for(uint32_t j=0;j<us;j++){int k=cat(br),x=0,v=0;for(int z=0;z<k;z++)x=(x<<1)|br.get();if(k)v=(x>=(1<<(k-1)))?x:x-((1<<k)-1);out.push_back(tr[v+128]);}}ofstream o("/tmp/M10R_unpacked.bin",ios::binary);o.write((char*)out.data(),out.size());return out.size()==expected?0:3;}
          CPP
          g++ -O3 -std=c++17 /tmp/unpack.cpp -o /tmp/unpack && /tmp/unpack
          python3 tools/m10r_sections.py /tmp/M10R_unpacked.bin /tmp/sections >/dev/null
          mkdir -p research
          python3 - <<'PY' > research/M10R_v028_meter_mode_names_ring_provenance.txt
          from pathlib import Path
          import csv,re,struct
          from capstone import Cs,CS_ARCH_ARM,CS_MODE_THUMB,CS_MODE_LITTLE_ENDIAN
          from capstone.arm import ARM_OP_IMM,ARM_OP_MEM,ARM_REG_PC

          rows=list(csv.DictReader(open('/tmp/sections/sections.csv',newline='',encoding='utf-8')))
          kws=('meter','spot','integral','multi-field','multifield','mfm','center-weight','centre-weight','weighted','exposure mode','brightness value','bv_cmos','bvcmos')
          print('################ METERING-RELATED STRINGS ################')
          string_hits=[]
          for r in rows:
            data=(Path('/tmp/sections')/r['file']).read_bytes(); base=int(r['image_base'],16)
            for m in re.finditer(rb'[ -~]{4,}',data):
              s=m.group().decode('latin1','replace'); low=s.lower()
              if any(k in low for k in kws):
                addr=base+m.start() if base else m.start()
                string_hits.append((int(r['index']),r['name'],base,m.start(),addr,s))
                print(r['index'],r['name'],f'base={base:#x}',f'off={m.start():#x}',f'addr={addr:#x}',repr(s))

          class Sec:
            def __init__(self,path,base,pfx=4):
              self.b=Path(path).read_bytes();self.base=base;self.pfx=pfx;self.code=self.b[pfx:]
              self.md=Cs(CS_ARCH_ARM,CS_MODE_THUMB|CS_MODE_LITTLE_ENDIAN);self.md.detail=True;self.md.skipdata=True
            def off(self,a):return self.pfx+a-self.base
            def u32(self,a):
              o=self.off(a);return struct.unpack_from('<I',self.b,o)[0] if 0<=o<=len(self.b)-4 else None
            def astr(self,a,n=180):
              o=self.off(a)
              if not 0<=o<len(self.b):return None
              out=[]
              for c in self.b[o:o+n]:
                if c==0:break
                if c in (9,10,13) or 32<=c<127:out.append(chr(c))
                else:return None
              s=''.join(out);return s if len(s)>=4 else None
            def fmt(self,i):
              ex=''
              if i.mnemonic.startswith('ldr') and len(i.operands)>1 and i.operands[1].type==ARM_OP_MEM and i.operands[1].mem.base==ARM_REG_PC:
                la=((i.address+4)&~3)+i.operands[1].mem.disp;v=self.u32(la)
                if v is not None:
                  s=self.astr(v);ex=f' ; LIT {la:#x}->{v:#x}'+(f' STRING={s!r}' if s else '')
              if i.mnemonic in ('bl','blx') and i.operands and i.operands[0].type==ARM_OP_IMM:ex+=f' ; CALL {i.operands[0].imm:#x}'
              return f'{i.address:08x}: {i.mnemonic:10s} {i.op_str}{ex}'
            def dump(self,a,z,title):
              print(f'\n=== {title} {a:#x}..{z:#x} ===')
              for i in self.md.disasm(self.b[self.off(a):self.off(z)],a):
                if i.mnemonic!='.byte':print(self.fmt(i))
            def str_xrefs(self,target):
              lits=[];pat=struct.pack('<I',target);q=0
              while True:
                q=self.b.find(pat,q)
                if q<0:break
                if q>=self.pfx:lits.append(self.base+q-self.pfx)
                q+=1
              refs=[]
              for la in lits:
                st=max(self.base,la-0x1000)&~1; en=min(self.base+len(self.code),la+4)
                for i in self.md.disasm(self.b[self.off(st):self.off(en)],st):
                  if i.mnemonic.startswith('ldr') and len(i.operands)>1 and i.operands[1].type==ARM_OP_MEM and i.operands[1].mem.base==ARM_REG_PC:
                    got=((i.address+4)&~3)+i.operands[1].mem.disp
                    if got==la:refs.append(i.address)
              return sorted(set(refs))

          ctrl=Sec('/tmp/sections/090_CTRL-System.bin',0x08000000)
          img=Sec('/tmp/sections/092_IMG-System.bin',0x42000000)
          print('\n################ STRING XREFS IN CTRL/IMG ################')
          for idx,name,base,off,addr,s in string_hits:
            sec=ctrl if idx==90 else img if idx==92 else None
            if not sec or not base:continue
            refs=sec.str_xrefs(addr)
            if refs:
              print('\nSTRING',repr(s),'ADDR',hex(addr),'REFS',[hex(x) for x in refs])
              for a in refs:sec.dump(max(sec.base,a-0x40),a+0x70,'STRING REF '+hex(a))

          print('\n################ SELECTOR AND LVVIDEOSYNC PATHS ################')
          ctrl.dump(0x0800f180,0x0800f2b0,'METERING SELECTOR / BVCMOS UPDATE')
          ctrl.dump(0x08003020,0x08003160,'LVVIDEOSYNC RING INGEST')
          ctrl.dump(0x080020ba,0x080020e2,'EXPOSURE METERING MODE RECEIVER')
          PY
      - name: Commit result
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add research/M10R_v028_meter_mode_names_ring_provenance.txt
          git diff --cached --quiet && exit 0
          git commit -m 'research: record v0.28 metering mode names and ring provenance'
          git pull --rebase origin m10r-metering-latch-trace
          git push origin HEAD:m10r-metering-latch-trace
