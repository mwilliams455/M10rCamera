name: M10-R v0.67 B2Y selector xref and call order
on:
  push:
    branches: [m10r-reference-transfer-rgb1e]
    paths:
      - 'tools/m10r_b2y_selector_order_v067.py'
      - '.github/workflows/m10r-b2y-selector-order-v067.yml'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: python3 -m pip install --quiet 'capstone==5.0.3'
      - name: Fetch verify decode extract
        shell: bash
        run: |
          set -euo pipefail
          curl -L --fail --retry 3 --retry-delay 2 'https://reddotforum.com/Firmware_files/M10-R/M10-R-30.22.23.34-Customer.FW' -o /tmp/M10-R.FW
          echo 'ec8df72dc7d4c90332abb3cdbc07c6db837ffed802c1a9712b6f3d6e0edf6498  /tmp/M10-R.FW' | sha256sum -c -
          python3 - <<'PY'
          from pathlib import Path
          p=Path('.github/workflows/m10r-v135-wb-mode-switch.yml').read_text()
          a=p.index("cat >/tmp/unpack.cpp <<'CPP'\n")+len("cat >/tmp/unpack.cpp <<'CPP'\n")
          z=p.index("\n          CPP",a)
          Path('/tmp/unpack.cpp').write_text(p[a:z])
          PY
          g++ -O3 -std=c++17 /tmp/unpack.cpp -o /tmp/unpack && /tmp/unpack
          echo '859c936b6ac8efe0bc4ce7f05623013644fc5d8aee3c6835f09860f452adc2e0  /tmp/M10R_unpacked.bin' | sha256sum -c -
          python3 tools/m10r_sections.py /tmp/M10R_unpacked.bin /tmp/sec >/dev/null
      - name: Trace selector refs and call order
        run: python3 tools/m10r_b2y_selector_order_v067.py /tmp/sec | tee research/M10R_v067_b2y_selector_order.txt
      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: M10R-V067-B2Y-SELECTOR-ORDER
          path: research/M10R_v067_b2y_selector_order.txt
      - name: Commit evidence
        run: |
          git config user.name 'github-actions[bot]'; git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add research/M10R_v067_b2y_selector_order.txt
          git commit -m 'research: record v067 B2Y selector xref and call order' || exit 0
          git pull --rebase origin m10r-reference-transfer-rgb1e && git push
